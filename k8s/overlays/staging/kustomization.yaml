apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Staging environment. Patches the shared base with staging-specific values
# (replica counts, domain, resource limits, image tags).
namespace: cosmos-staging

resources:
  - ../../base

# patches:
#   - path: replicas.yaml
